config:
  target: 'http://localhost:5001'
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
      Content-Type: application/json

scenarios:
  - flow:
      - get:
          url: '/api/stations'
